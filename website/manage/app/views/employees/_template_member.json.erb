{
  "id": <%= raw(employee.id.to_json) %>,
  "first_name": <%= raw(employee.first_name.to_json) %>,
  "middle_name": <%= raw(employee.middle_name.to_json) %>,
  "last_name": <%= raw(employee.last_name.to_json) %>,
  "email": <%= raw(employee.email.to_json) %>,
  "job_position_id": <%= raw(employee.job_position_id.to_json) %>
}