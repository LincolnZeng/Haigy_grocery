{
  "service_area_id": <%= raw(@service_area_id.to_json) %>,

  "unavailable_items_id": <%= raw(@unavailableItemsIdArray.to_json) %>,

  "selected_address": {
  	<%= render partial: "address_attribute", locals: {address: @userAddress} %>
	}
}